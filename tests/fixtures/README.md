# Synthetic Test Fixtures

All fixtures used by tests in this repository are synthetic/anonymized.

- No real memo decks, proformas, schedules, credentials, or client data are
  included here.
- Most fixtures are generated at test runtime in `conftest.py` (temporary
  `.pptx` and `.xlsx` files).
- This directory exists to document fixture policy for contributors and CI.
